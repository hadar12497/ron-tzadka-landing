# איך לעבוד עם Codex על אתר פרימיום עם 3D

המטרה של הפרויקט הזה היא ללמוד דרך בנייה אמיתית, לא דרך תאוריה בלבד. בכל פעם שאתה עובד איתי על אתר כזה, תחשוב בשלושה רבדים: עסק, עיצוב, וטכנולוגיה.

## מה נבנה כאן

- אתר מכירה לרון צדקה, מאמן כושר ובעל סטודיו לאימוני כוח וחיטוב בלהבים.
- עמוד מכירה מלא: Hero, אמינות, שיטה, תוכניות, case study וקריאה לפעולה.
- רקע 3D אינטראקטיבי ב-Hero עם `React Three Fiber`.
- fallback קל למובייל או למשתמשים שמעדיפים פחות תנועה.

## איך לבקש ממני שדרוגים

דוגמאות לבקשות טובות:

```text
תשפר את ה-Hero כך שירגיש יותר יוקרתי, אבל בלי להעמיס צבעים.
```

```text
תסביר לי את components/HeroScene.tsx כאילו אני מתחיל, ואז תן לי תרגיל קטן לשנות את הסצנה.
```

```text
תבדוק את האתר במובייל ודסקטופ, ותתקן בעיות של טקסט, ריווחים או 3D שמסתיר תוכן.
```

```text
תוסיף גרסת לקוח אמיתית: טופס לידים, וואטסאפ, ותוכן שמתאים לסטודיו כושר בישראל.
```

## פקודות שימושיות

```bash
pnpm dev
pnpm build
pnpm lint
```

## מה ללמוד מהקוד

- `app/page.tsx`: מבנה עסקי של עמוד מכירה.
- `components/HeroScene.tsx`: איך מחברים 3D ל-React ואיך שומרים fallback למובייל.
- `app/api/contact/route.ts`: איך טופס אמיתי שולח ליד דרך שירות אימייל.
- `components/ContactForm.tsx`: איך בונים טופס עם טעינה, הצלחה ושגיאה.
- `components/Reveal.tsx`: אנימציות כניסה עדינות עם Motion.
- `app/globals.css`: עיצוב רספונסיבי, טיפוגרפיה, צבעים ומניעת שבירת UI.

## איך להפעיל שליחת מייל אמיתית

הטופס מחובר ל-API route אמיתי, אבל כדי לשלוח אימיילים חייבים להגדיר שירות שליחה. הדרך המומלצת:

1. פתח חשבון ב-Resend.
2. צור API key.
3. העתק את `.env.example` לקובץ `.env.local`.
4. מלא:

```bash
RESEND_API_KEY=...
CONTACT_TO_EMAIL=your@email.com
CONTACT_FROM_EMAIL=Ron Tzadka <hello@your-verified-domain.com>
```

בלי המשתנים האלה הטופס יחזיר הודעת שגיאה ברורה במקום להעמיד פנים שהוא שלח.
